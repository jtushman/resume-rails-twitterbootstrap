<h1>Video</h1>

<p>
  <b>Name</b><br>
  <%= @video.name %>
</p>

<div class="form-actions">
  <%= link_to 'Back', videos_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_video_path(@video), :class => 'btn' %>
  <%= link_to 'Delete', video_path(@video), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>
